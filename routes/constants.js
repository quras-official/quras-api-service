var define = require('node-constants')(exports);

define("CONSOLELOG_FLAG", 1);

define("ERR_CONSTANTS",{
	success				: 0,
  db_connection_err		: 101,
  db_not_found_err    : 102,

  api_param_format_err  : 201,
});